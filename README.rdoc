== ORS API Demo application

This project is a single page infographic to demonstrate the potential use of the ORS API. The project is pulling real data from our Online Reporting System using the API.

No special setup, just clone, cd into the directory, `bundle install` and `rails s` to start
